[
  {
    "id": 1,
    "titre": "Configurer base projet React+TS+Tailwind+Vite",
    "prompt": "## Contexte\nRepo: `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39` (Windows 11, PowerShell). Le repo contient deja un `package.json` initialise mais incomplet, ainsi que des fichiers read-only a NE PAS toucher (`cahier_des_charges_radioprotection_v2.md`, `schema.sql`, `design-mockup/**`, `STRUCTURE.md`, `LICENSE`, `.gitignore`, `README.md`).\n\nObjectif: poser la fondation frontend (React 18 + TypeScript + Tailwind 3 + Vite) avec tokens de design OKLCH et fonts Public Sans / JetBrains Mono. Aucune installation reseau n'est requise dans la tache si ton outil ne le permet pas: tu peux generer/mettre a jour `package.json` puis lancer `npm install` une seule fois.\n\n## SCOPE STRICT (fichiers autorises a creer ou editer, et RIEN d'autre)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\package.json`\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\tsconfig.json`\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\tsconfig.node.json`\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\vite.config.ts`\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\tailwind.config.ts`\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\postcss.config.js`\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\index.html`\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src\\main.tsx`\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src\\App.tsx` (placeholder minimal: `<div>PCR Manager</div>`)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src\\index.css`\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src\\vite-env.d.ts`\n\nINTERDIT: toucher a tout autre fichier; aucun refactor de fichiers existants hors `package.json`; ne pas creer de fichiers Rust/Tauri (autre tache); ne pas creer de dossier `src/components`, `src/modules`, etc. Si tu reperes un bug hors-scope, RAPPORTE-le sans le corriger.\n\n## Edits attendus (precis)\n1. `package.json`: champs `name=pcr-manager`, `private=true`, `type=module`, scripts (`dev`, `build`, `preview`, `typecheck` => `tsc --noEmit`). Dependencies: `react@^18`, `react-dom@^18`, `react-router-dom@^6`, `@tanstack/react-query@^5`, `lucide-react`. DevDependencies: `typescript@^5`, `@types/react`, `@types/react-dom`, `@types/node`, `vite@^5`, `@vitejs/plugin-react`, `tailwindcss@^3`, `postcss`, `autoprefixer`, `@tauri-apps/api@^2`, `@tauri-apps/cli@^2`.\n2. `tsconfig.json`: `target=ES2022`, `module=ESNext`, `moduleResolution=Bundler`, `jsx=react-jsx`, `strict=true`, `noEmit=true`, `isolatedModules=true`, `esModuleInterop=true`, `skipLibCheck=true`, `lib=[ES2022,DOM,DOM.Iterable]`, `include=[src]`, `references=[{ \"path\": \"./tsconfig.node.json\" }]`.\n3. `tsconfig.node.json`: config pour vite.config.ts (`composite=true`, `module=ESNext`, `moduleResolution=Bundler`, include `vite.config.ts`).\n4. `vite.config.ts`: `defineConfig` avec plugin `@vitejs/plugin-react`, `clearScreen=false`, `server.port=1420`, `server.strictPort=true`.\n5. `tailwind.config.ts`: `content: ['./index.html','./src/**/*.{ts,tsx}']`. Etendre `theme.extend.colors` avec une palette nommee (`bg`, `surface`, `surface2`, `surfaceHover`, `border`, `borderStrong`, `text`, `textMuted`, `textSoft`, `accent`, `accentHover`, `accentSoft`, `accentSoftBorder`, `ok`, `okBg`, `okBorder`, `warn`, `warnBg`, `warnBorder`, `danger`, `dangerBg`, `dangerBorder`, `neutral`, `neutralBg`, `neutralBorder`) chacune mappee a `var(--<token>)`. `fontFamily.sans=['\"Public Sans\"', 'system-ui', 'sans-serif']`, `fontFamily.mono=['\"JetBrains Mono\"', 'ui-monospace', 'monospace']`. `borderRadius.DEFAULT='6px'`, `borderRadius.sm='4px'`.\n6. `postcss.config.js`: `export default { plugins: { tailwindcss: {}, autoprefixer: {} } }`.\n7. `index.html`: lang=fr, charset UTF-8, viewport, title `PCR Manager`, `<link rel=\"preconnect\">` et `<link href=\"https://fonts.googleapis.com/css2?family=Public+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap\" rel=\"stylesheet\">`, body avec `<div id=\"root\"></div>` et `<script type=\"module\" src=\"/src/main.tsx\"></script>`.\n8. `src/index.css`: directives `@tailwind base; @tailwind components; @tailwind utilities;` puis un bloc `:root { ... }` avec TOUTES les variables CSS OKLCH listees dans le brief (`--bg`, `--surface`, `--surface-2`, `--surface-hover`, `--border`, `--border-strong`, `--text`, `--text-muted`, `--text-soft`, `--accent`, `--accent-hover`, `--accent-soft`, `--accent-soft-border`, `--ok`, `--ok-bg`, `--ok-border`, `--warn`, `--warn-bg`, `--warn-border`, `--danger`, `--danger-bg`, `--danger-border`, `--neutral`, `--neutral-bg`, `--neutral-border`, `--radius`, `--radius-sm`) avec les valeurs OKLCH exactes du brief. Ajouter regle globale `body { font-family: 'Public Sans', system-ui, sans-serif; font-size: 14px; line-height: 1.45; font-feature-settings: 'ss01','cv11'; background: var(--bg); color: var(--text); }` et classe utilitaire `.mono { font-family: 'JetBrains Mono', ui-monospace, monospace; font-variant-numeric: tabular-nums; font-feature-settings: 'zero'; }`. Le mapping Tailwind colors->`var(--*)` doit etre coherent (`bg` => `var(--bg)`, `surface2` => `var(--surface-2)`, `accentSoftBorder` => `var(--accent-soft-border)`, etc.).\n9. `src/main.tsx`: bootstrap React 18 avec `createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>)`, import `./index.css`.\n10. `src/App.tsx`: composant `App` rendu minimaliste `<div className=\"p-8\">PCR Manager — bootstrap OK</div>`.\n11. `src/vite-env.d.ts`: `/// <reference types=\"vite/client\" />`.\n\n## Critere d'acceptation\n- Tous les fichiers ci-dessus sont presents avec contenu valide.\n- `npm install` se termine sans erreur fatale.\n- `npx tsc --noEmit` ne sort aucune erreur (warnings tolerables).\n\n## Commande de validation (a executer obligatoirement en fin de tache, depuis le repo)\n```powershell\nnpm install ; npx tsc --noEmit\n```\nSi `npm install` echoue pour cause reseau, rapporter precisement l'erreur sans modifier d'autres fichiers.\n\n## Rapport final (3 lignes max)\nLigne 1: liste des fichiers crees/modifies. Ligne 2: resultat de `npx tsc --noEmit` (OK ou nombre d'erreurs + 1er message). Ligne 3: anomalies hors-scope reperees mais NON corrigees."
  },
  {
    "id": 2,
    "titre": "Initialiser shell Tauri 2 Rust avec commande ping",
    "prompt": "## Contexte\nRepo: `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39` (Windows 11). Tu dois creer la coquille Rust/Tauri 2 minimale avec une commande de test `ping` exposee au frontend. La tache precedente a deja prepare le frontend (React+TS+Tailwind+Vite). Tu N'AS PAS a relancer `npm install`. Tu N'AS PAS a lancer `tauri dev` ni `tauri build`.\n\nFichiers read-only INTOUCHABLES: `cahier_des_charges_radioprotection_v2.md`, `schema.sql`, `design-mockup/**`, `STRUCTURE.md`, `LICENSE`, `.gitignore`, `README.md`, plus tous les fichiers crees par la tache 1 (`package.json`, `tsconfig.json`, `vite.config.ts`, `tailwind.config.ts`, `postcss.config.js`, `index.html`, `src/index.css`, `src/main.tsx`, `src/App.tsx`, `src/vite-env.d.ts`, `tsconfig.node.json`).\n\n## SCOPE STRICT (uniquement ces fichiers)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\Cargo.toml`\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\tauri.conf.json`\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\build.rs`\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\main.rs`\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\lib.rs` (si necessaire pour Tauri 2)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\icons\\.gitkeep`\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\.gitignore`\n\nINTERDIT: editer le `package.json` racine, le `.gitignore` racine, ou tout autre fichier. Pas de cargo install global. Pas de creation d'icones binaires (laisser `.gitkeep` placeholder). Si Haiku reperе que `package.json` racine devrait declarer un script `tauri`, RAPPORTER sans editer.\n\n## Edits attendus (precis)\n1. `src-tauri/Cargo.toml`:\n   ```toml\n   [package]\n   name = \"pcr-manager\"\n   version = \"0.1.0\"\n   edition = \"2021\"\n   description = \"PCR Manager — suivi radioprotection\"\n   authors = [\"PCR Manager\"]\n   \n   [build-dependencies]\n   tauri-build = { version = \"2\", features = [] }\n   \n   [dependencies]\n   tauri = { version = \"2\", features = [] }\n   serde = { version = \"1\", features = [\"derive\"] }\n   serde_json = \"1\"\n   ```\n   Ajouter `[lib]` et `[[bin]]` standards Tauri 2: `[lib] name = \"pcr_manager_lib\" crate-type = [\"staticlib\", \"cdylib\", \"rlib\"]`. `[[bin]] name = \"pcr-manager\" path = \"src/main.rs\"`.\n2. `src-tauri/tauri.conf.json`: identifier `com.pcrmanager.app`, productName `PCR Manager`, version `0.1.0`. Bloc `build`: `frontendDist=\"../dist\"`, `devUrl=\"http://localhost:1420\"`, `beforeDevCommand=\"npm run dev\"`, `beforeBuildCommand=\"npm run build\"`. Bloc `app.windows`: 1 fenetre `main`, title `PCR Manager`, width 1280, height 800, resizable true. Bloc `bundle`: `active=true`, `targets=[\"nsis\",\"dmg\"]`, `icon=[]` (vide accepte au stade prototype).\n3. `src-tauri/build.rs`: `fn main() { tauri_build::build() }`.\n4. `src-tauri/src/lib.rs`: definir `#[tauri::command] fn ping() -> &'static str { \"pong\" }` et fonction publique `pub fn run() { tauri::Builder::default().invoke_handler(tauri::generate_handler![ping]).run(tauri::generate_context!()).expect(\"erreur Tauri\"); }`.\n5. `src-tauri/src/main.rs`: contenu minimal `#![cfg_attr(not(debug_assertions), windows_subsystem = \"windows\")] fn main() { pcr_manager_lib::run(); }`.\n6. `src-tauri/icons/.gitkeep`: fichier vide.\n7. `src-tauri/.gitignore`: contenu standard Tauri (`target/`, `Cargo.lock` non ignore en binaire, mais pour bibliotheque l'ignorer; OK ignorer `target/` uniquement).\n\nIMPORTANT: si Tauri 2 requiert des versions exactes differentes (ex. `tauri = \"2.0.0\"`), utiliser les ranges les plus permissifs (`\"2\"`). Si l'install cargo signale qu'il manque une feature obligatoire (ex. `tauri/protocol-asset`), RAPPORTER sans editer hors-scope.\n\n## Critere d'acceptation\n- L'arborescence `src-tauri/` existe avec les fichiers ci-dessus.\n- `cargo check --manifest-path src-tauri/Cargo.toml` se termine sans erreur (warnings tolerables). Le premier `cargo check` telechargera les crates — si pas de reseau, RAPPORTER l'erreur reseau precise sans rien modifier.\n\n## Commande de validation\n```powershell\ncargo check --manifest-path \"C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\Cargo.toml\"\n```\n\n## Rapport final (3 lignes max)\nLigne 1: fichiers crees. Ligne 2: code retour `cargo check` (OK ou 1er message d'erreur). Ligne 3: anomalies hors-scope NON corrigees."
  },
  {
    "id": 3,
    "titre": "Ajouter migrations SQLCipher et module db.rs",
    "prompt": "## Contexte\nRepo: `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39`. Tu dois (a) copier le contenu de `schema.sql` du repo dans `src-tauri/migrations/V1__initial.sql` en REORDONNANT pour que la table `appareil` soit creee AVANT `competence_travailleur` (qui la reference par FK), (b) creer un module Rust `db.rs` qui ouvre une connexion `rusqlite` avec SQLCipher (feature `bundled-sqlcipher-vendored-openssl`), applique PRAGMA `key`, `foreign_keys=ON`, `journal_mode=WAL`, execute la migration V1, et expose une commande Tauri `init_db` qui retourne `Ok(())`.\n\nFichiers read-only INTOUCHABLES en plus de ceux de la liste globale: ne JAMAIS modifier `schema.sql` du repo (lire seulement). Ne pas toucher aux fichiers Tauri/Frontend crees par les taches 1-2 HORS de la liste ci-dessous.\n\n## SCOPE STRICT (uniquement ces fichiers)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\migrations\\V1__initial.sql` (CREER)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\db.rs` (CREER)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\Cargo.toml` (EDITER pour ajouter dependances)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\lib.rs` (EDITER pour declarer `mod db;` et ajouter la commande `init_db` au handler ainsi que le state)\n\nINTERDIT: toucher `schema.sql` racine, ou tout autre fichier (notamment ne pas creer le module `auth.rs` ou `commands/` — c'est pour les taches suivantes).\n\n## Edits attendus\n1. `Cargo.toml`: ajouter aux `[dependencies]`:\n   - `rusqlite = { version = \"0.31\", features = [\"bundled-sqlcipher-vendored-openssl\", \"chrono\"] }` (si version refusee, prendre la plus recente compatible avec la feature)\n   - `chrono = { version = \"0.4\", features = [\"serde\"] }`\n   - `anyhow = \"1\"`\n   - `thiserror = \"1\"`\n   - `directories = \"5\"` (pour resoudre `AppData` cross-platform)\n   - `parking_lot = \"0.12\"` (pour Mutex sans poisoning)\n2. `migrations/V1__initial.sql`: copier IDENTIQUEMENT le contenu de `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\schema.sql` (a LIRE d'abord obligatoirement) en deplacant le bloc `CREATE TABLE appareil (...)` AVANT le bloc `CREATE TABLE competence_travailleur (...)`. Conserver tous les autres elements (vues, triggers, seed `competence_ref`, etc.) a l'identique. Ajouter en TETE du fichier:\n   ```sql\n   PRAGMA foreign_keys = ON;\n   PRAGMA journal_mode = WAL;\n   ```\n3. `src-tauri/src/db.rs`:\n   - struct publique `DbState { conn: parking_lot::Mutex<rusqlite::Connection> }`.\n   - fonction `pub fn open_db(app_handle: &tauri::AppHandle) -> anyhow::Result<rusqlite::Connection>`: resout le chemin `{AppLocalData}/PCR Manager/pcr.db` via `app_handle.path()` (API Tauri 2 — si l'API exacte differe, utiliser la fonction Tauri 2 officielle pour AppLocalData; ne pas inventer de chemin code en dur). Si le fichier parent n'existe pas, le creer. Ouvrir la connexion, executer `PRAGMA key = 'CHANGEME_DEV_KEY';` (TODO documente: lecture depuis keychain a faire dans une tache ulterieure — un commentaire `// TODO: derive key from OS keychain` est autorise ici).\n   - fonction `pub fn run_migrations(conn: &mut rusqlite::Connection) -> anyhow::Result<()>`: lit le SQL embarque via `include_str!(\"../migrations/V1__initial.sql\")` et l'execute via `conn.execute_batch(sql)`. Idempotence: encadrer par une table `__migrations(version INTEGER PRIMARY KEY)` — si V1 deja appliquee, ne pas reexecuter.\n   - `#[tauri::command] pub async fn init_db(state: tauri::State<'_, DbState>) -> Result<(), String>`: utilise la connexion en state pour appeler `run_migrations`. Convertir les erreurs en `String` via `e.to_string()`.\n4. `src-tauri/src/lib.rs`: ajouter `mod db;` en haut. Dans `run()`, avant `.run(...)`, ouvrir la DB via `tauri::Builder::default().setup(|app| { let conn = db::open_db(app.handle())?; let mut conn = conn; db::run_migrations(&mut conn)?; app.manage(db::DbState { conn: parking_lot::Mutex::new(conn) }); Ok(()) })`. Ajouter `db::init_db` au `generate_handler!` aux cotes de `ping`.\n\nSi tu doutes du nom exact d'une API Tauri 2 (ex. resolution de chemin AppLocalData), LIRE d'abord `Cargo.toml` et la doc embarquee (`cargo doc` non requis). Sinon, utiliser un commentaire indiquant que l'appel doit pointer vers le dossier AppLocalData officiel via l'API Tauri 2 (`app.path().app_local_data_dir()` typiquement).\n\n## Critere d'acceptation\n- `V1__initial.sql` existe et `appareil` apparait avant `competence_travailleur` (verifiable par grep du numero de ligne).\n- `cargo check --manifest-path src-tauri/Cargo.toml` reussit (le premier `cargo check` compilera OpenSSL vendored — long mais doit reussir; si echec compilation OpenSSL, rapporter precisement et NE PAS contourner).\n\n## Commande de validation\n```powershell\ncargo check --manifest-path \"C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\Cargo.toml\"\n```\n\n## Rapport final (3 lignes max)\nLigne 1: fichiers crees/edites. Ligne 2: resultat `cargo check`. Ligne 3: anomalies hors-scope reperees mais NON corrigees."
  },
  {
    "id": 4,
    "titre": "Implementer authentification Passkey WebAuthn cote Rust",
    "prompt": "## Contexte\nRepo: `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39`. Tu dois ajouter un module `auth.rs` qui implemente l'enregistrement et l'authentification d'une passkey via la crate `webauthn-rs`, stockee dans la table `passkey` de la base SQLCipher. Les commandes `passkey_register_start`, `passkey_register_finish`, `passkey_auth_start`, `passkey_auth_finish` doivent etre exposees a Tauri.\n\nTu n'as PAS la signature exacte des champs de la table `passkey` ni du DbState. Tu DOIS donc:\n1. LIRE `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\migrations\\V1__initial.sql` pour identifier les colonnes exactes de la table `passkey`.\n2. LIRE `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\db.rs` pour reutiliser le DbState existant sans inventer de variante.\n3. LIRE `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\lib.rs` pour comprendre comment les commandes y sont enregistrees.\n\nNe pas inventer de champ DB qui n'existe pas. Si une colonne necessaire manque, RAPPORTER sans modifier la migration.\n\n## SCOPE STRICT\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\auth.rs` (CREER)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\Cargo.toml` (EDITER deps)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\lib.rs` (EDITER: `mod auth;` + register handlers + state webauthn)\n\nINTERDIT: modifier `db.rs`, `migrations/`, ou tout autre fichier. Pas de refactor de l'existant.\n\n## Edits attendus\n1. `Cargo.toml`: ajouter `webauthn-rs = { version = \"0.5\", features = [\"danger-allow-state-serialisation\"] }`, `uuid = { version = \"1\", features = [\"v4\", \"serde\"] }`, `base64 = \"0.22\"`.\n2. `auth.rs`:\n   - Construire un `Webauthn` (rp_id `pcrmanager.local`, rp_origin `tauri://localhost` — si `webauthn-rs` exige une URL valide en prod, accepter une origine de dev en `https://localhost`). Mettre dans un `WebauthnState { webauthn: Arc<Webauthn>, reg_states: parking_lot::Mutex<HashMap<String, PasskeyRegistration>>, auth_states: parking_lot::Mutex<HashMap<String, PasskeyAuthentication>> }`.\n   - 4 commandes `#[tauri::command]` async retournant `Result<serde_json::Value, String>` :\n     - `passkey_register_start(state: State<WebauthnState>) -> CreationChallengeResponse`\n     - `passkey_register_finish(state: State<WebauthnState>, db: State<DbState>, reg_id: String, response: RegisterPublicKeyCredential)` => insert dans table `passkey` (utiliser EXCLUSIVEMENT les colonnes vues dans la migration; serialiser le credential via `serde_json::to_string` + base64).\n     - `passkey_auth_start(state: State<WebauthnState>, db: State<DbState>) -> RequestChallengeResponse` => recharge toutes les passkeys de la table.\n     - `passkey_auth_finish(state: State<WebauthnState>, db: State<DbState>, auth_id: String, response: PublicKeyCredential)` => verifie et met a jour le `counter` si la table le prevoit (verifier d'abord les colonnes lues).\n   - Mapping erreurs => `String` via `.map_err(|e| e.to_string())`.\n3. `lib.rs`: ajouter `mod auth;`. Construire `auth::WebauthnState` dans le `setup` et l'enregistrer via `app.manage(...)`. Ajouter les 4 commandes dans `generate_handler![ping, init_db, passkey_register_start, passkey_register_finish, passkey_auth_start, passkey_auth_finish]`.\n\nSi `webauthn-rs` ne fournit pas le trait/type attendu (changement d'API), suivre la doc embarquee dans `cargo doc --open` n'est PAS demande — utiliser les types publics standards documentes en commentaire du crate; si bloque, RAPPORTER l'erreur exacte sans inventer.\n\n## Critere d'acceptation\n- `cargo check --manifest-path src-tauri/Cargo.toml` reussit.\n- Les 4 commandes sont declarees dans `generate_handler!`.\n- Aucune colonne inventee: chaque INSERT/SELECT n'utilise que des colonnes presentes dans `V1__initial.sql`.\n\n## Commande de validation\n```powershell\ncargo check --manifest-path \"C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\Cargo.toml\"\n```\n\n## Rapport final (3 lignes max)\nLigne 1: fichiers crees/edites + colonnes effectivement utilisees de `passkey`. Ligne 2: resultat `cargo check`. Ligne 3: anomalies hors-scope NON corrigees."
  },
  {
    "id": 5,
    "titre": "Creer modeles Rust et commandes CRUD par module",
    "prompt": "## Contexte\nRepo: `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39`. Tu dois creer les structures Rust mirror du schema SQL et les commandes Tauri CRUD pour chaque module metier. Tu N'AS PAS le schema en tete: tu DOIS d'abord LIRE `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\migrations\\V1__initial.sql` pour relever les colonnes exactes et leurs types. Aucune invention de champ, aucun renommage.\n\nEgalement LIRE `src-tauri/src/db.rs` et `src-tauri/src/lib.rs` pour reutiliser le `DbState` deja en place sans en creer une variante.\n\n## SCOPE STRICT\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\models.rs` (CREER)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\commands\\mod.rs` (CREER)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\commands\\etablissement.rs` (CREER)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\commands\\travailleur.rs` (CREER)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\commands\\habilitation.rs` (CREER)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\commands\\appareil.rs` (CREER)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\commands\\competence.rs` (CREER)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\commands\\verification.rs` (CREER)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\commands\\controle_qualite.rs` (CREER)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\commands\\document.rs` (CREER)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\lib.rs` (EDITER: declarer `mod models; mod commands;` et enregistrer toutes les nouvelles commandes dans `generate_handler!`)\n\nINTERDIT: modifier `migrations/`, `db.rs`, `auth.rs`. Pas de `schema.sql` original. Pas de refactor d'autre fichier.\n\n## Edits attendus\n1. `models.rs`: une struct `#[derive(Serialize, Deserialize, Debug, Clone)]` par table metier (`Etablissement`, `Travailleur`, `Habilitation`, `CompetenceRef`, `CompetenceTravailleur`, `Appareil`, `VerificationTechnique`, `ControleQualite`, `Document`). Chaque struct miroir EXACT des colonnes du schema (nom et type Rust adapte: `i64` pour INTEGER, `String` pour TEXT, `Option<...>` pour colonnes nullable, `chrono::NaiveDate` ou `String` pour dates ISO — privilegier `String` pour rester proche du stockage SQLite et eviter dependance chrono dans le serde frontier).\n2. `commands/mod.rs`: declarations `pub mod etablissement; pub mod travailleur; ...` (8 modules listes ci-dessus).\n3. Chaque fichier `commands/<entite>.rs` expose au MINIMUM les commandes Tauri `<entite>_list`, `<entite>_get`, `<entite>_create`, `<entite>_update`, `<entite>_delete` (sauf pour `habilitation.rs` qui n'est PAS persistee — voir plus bas; et `competence.rs` qui gere `competence_list`, `competence_set` (upsert d'un binding travailleur/appareil/competence_ref), `competence_get_for_travailleur`).\n4. `commands/habilitation.rs`: commande unique `habilitation_compute(travailleur_id: i64) -> HabilitationStatus` qui interroge les autres tables pour calculer le statut (formation RP a jour, dosimetries renseignees, competences appareil >=1 validation complete 9/9, visite med non expiree). Si une donnee manque, retourner `non_validee`. Logique exacte: voir brief metier. Retourner une struct `{ statut: \"validee\"|\"partielle\"|\"non_validee\", details: { formation_rp_ok: bool, dosimetries_ok: bool, competences_ok: bool, visite_med_ok: bool } }`.\n5. `commands/document.rs`: commande `document_upload(entite_type: String, entite_id: i64, source_path: String) -> Document` qui copie le fichier source vers `{AppLocalData}/PCR Manager/documents/{uuid}.{ext}` (utiliser `directories` ou l'API Tauri 2 `app.path().app_local_data_dir()` deja utilisee dans `db.rs`) et insere une ligne dans la table `document`. Commande `document_get(id)` et `document_delete(id)` aussi.\n6. Toutes les requetes SQL utilisent uniquement des colonnes lues dans la migration. Echec => `Result<T, String>` via `e.to_string()`.\n7. `lib.rs`: ajouter `mod models; mod commands;` et integrer chaque commande au `generate_handler!` (etendre la liste existante).\n\n## Critere d'acceptation\n- `cargo check --manifest-path src-tauri/Cargo.toml` passe.\n- Toutes les commandes listees ci-dessus sont declarees dans `generate_handler!`.\n- Aucune colonne SQL inventee (verifier en comparant chaque INSERT/SELECT au schema lu).\n\n## Commande de validation\n```powershell\ncargo check --manifest-path \"C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\Cargo.toml\"\n```\n\n## Rapport final (3 lignes max)\nLigne 1: liste des commandes Tauri enregistrees (compteur). Ligne 2: resultat `cargo check`. Ligne 3: anomalies hors-scope NON corrigees."
  },
  {
    "id": 6,
    "titre": "Implementer les composants UI primitifs Tailwind",
    "prompt": "## Contexte\nRepo: `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39`. Tu dois implementer les composants UI primitifs reutilisables en React+TS+Tailwind, conformes a la maquette `design-mockup/` (read-only, source de verite visuelle). Avant d'ecrire les composants, INSPECTE `design-mockup/project/src/*.jsx` et `design-mockup/project/index.html` pour relever classes, paddings et structures exactes (couleurs, paddings, font-sizes). Reproduire pixel-perfect sans copier la structure prototype.\n\nLes tokens Tailwind (`accent`, `surface`, `text`, etc.) sont DEJA mappes aux variables CSS dans `tailwind.config.ts` et `src/index.css` (fait par tache 1). Utilise EXCLUSIVEMENT ces couleurs Tailwind (pas de hex en dur dans le JSX).\n\n## SCOPE STRICT\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src\\components\\ui\\Button.tsx`\n- `...\\src\\components\\ui\\Card.tsx`\n- `...\\src\\components\\ui\\Badge.tsx`\n- `...\\src\\components\\ui\\Dot.tsx`\n- `...\\src\\components\\ui\\Table.tsx`\n- `...\\src\\components\\ui\\KpiTile.tsx`\n- `...\\src\\components\\ui\\FormField.tsx`\n- `...\\src\\components\\ui\\Tabs.tsx`\n- `...\\src\\components\\ui\\PillFilter.tsx`\n- `...\\src\\components\\ui\\index.ts` (re-exports)\n- `...\\src\\components\\icons.tsx`\n- `...\\src\\lib\\cn.ts` (helper concat classes)\n\nINTERDIT: modifier `tailwind.config.ts`, `src/index.css`, `App.tsx`, ou tout fichier Rust. Pas de creation de modules metier. Pas de `Sidebar`/`Topbar` (tache suivante).\n\n## Edits attendus\n1. `lib/cn.ts`: helper `export function cn(...args: (string | false | null | undefined)[]) { return args.filter(Boolean).join(' '); }`.\n2. `Button.tsx`: props `{ variant?: 'default'|'primary'|'ghost'|'dangerGhost'; size?: 'default'|'sm'|'icon'; } & React.ButtonHTMLAttributes<HTMLButtonElement>`. Variants:\n   - default: bg-surface border border-border text-text hover:bg-surfaceHover\n   - primary: bg-accent text-white hover:bg-accentHover\n   - ghost: bg-transparent text-text hover:bg-surfaceHover\n   - dangerGhost: bg-transparent text-danger hover:bg-dangerBg\n   Sizes: default `h-9 px-3 text-sm`, sm `h-8 px-2.5 text-xs`, icon `h-9 w-9`. Radius `rounded`.\n3. `Card.tsx`: composant compose: `Card`, `CardHead`, `CardBody`, `CardTitle`. Card: `bg-surface border border-border rounded-lg shadow-sm`. CardHead: `px-5 py-4 border-b border-border flex items-center justify-between`. CardBody: `p-5`.\n4. `Badge.tsx`: props `{ variant?: 'ok'|'warn'|'danger'|'neutral'|'accent'; icon?: React.ReactNode }`. Pastille `inline-flex items-center gap-1.5 rounded-full px-2.5 py-0.5 text-xs font-medium`. Mapping background/border par variant via tokens (`bg-okBg border border-okBorder text-ok`, etc.).\n5. `Dot.tsx`: pastille 8x8 (`h-2 w-2 rounded-full inline-block`), couleur selon variant (`ok|warn|danger|neutral`).\n6. `Table.tsx`: wrapper `<table className=\"w-full text-sm\">` + sous-composants `THead`, `TBody`, `TR` (hover bg-surfaceHover), `TH` (uppercase text-xs text-textSoft font-medium px-3 py-2 border-b border-border text-left), `TD` (px-3 py-2 border-b border-border). Support classe `.mono` pour cellules numeriques.\n7. `KpiTile.tsx`: props `{ label: string; value: string | number; footer?: React.ReactNode }`. Label uppercase text-xs text-textSoft tracking-wide, valeur `text-3xl font-bold mono`, footer text-xs text-textMuted.\n8. `FormField.tsx`: composition `Field`, `Label`, `Input`, `Select`, `Textarea`. Label `text-xs font-medium text-textMuted mb-1.5`. Input/Select/Textarea: `w-full h-9 px-3 rounded border border-border bg-surface text-text focus:outline-none focus:border-accent focus:ring-2 focus:ring-accentSoft`.\n9. `Tabs.tsx`: composition `Tabs`, `TabList` (`flex border-b border-border`), `Tab` (props `active: boolean`; classe inactive `px-4 py-2.5 text-sm text-textMuted hover:text-text border-b-2 border-transparent`, active `text-accent border-accent`), `TabPanel`.\n10. `PillFilter.tsx`: props `{ options: { value: string; label: string }[]; value: string; onChange: (v: string) => void }`. Groupe segmente: container `inline-flex bg-surface2 border border-border rounded p-1`; boutons `px-3 py-1.5 text-sm rounded`; actif `bg-surface shadow-sm text-text`; inactif `text-textMuted hover:text-text`.\n11. `icons.tsx`: re-export selectif depuis `lucide-react` (`Search`, `Plus`, `Pencil`, `Trash2`, `Check`, `X`, `ChevronDown`, `ChevronRight`, `Calendar`, `AlertTriangle`, `ShieldCheck`, `Wrench`, `Activity`, `Users`, `Building2`, `LayoutDashboard`, `ListChecks`, `Upload`, `Download`). Si une icone listee n'existe pas dans lucide-react, REMPLACER par un equivalent existant et le mentionner dans le rapport.\n12. `ui/index.ts`: re-exporter tous les composants ci-dessus pour import groupe.\n\n## Critere d'acceptation\n- `npx tsc --noEmit` passe sans erreur.\n- Tous les composants exportent un default OU named export coherent avec les imports prevus dans `ui/index.ts`.\n- Aucune couleur en `#hex` ou `rgb()` dans les JSX (uniquement classes Tailwind mappees tokens).\n\n## Commande de validation\n```powershell\nnpx tsc --noEmit\n```\n\n## Rapport final (3 lignes max)\nLigne 1: composants livres + total. Ligne 2: resultat `tsc`. Ligne 3: icones renommees ou anomalies hors-scope NON corrigees."
  },
  {
    "id": 7,
    "titre": "Construire AppShell sidebar topbar et routing",
    "prompt": "## Contexte\nRepo: `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39`. Tu dois construire la coquille applicative: layout grid `240px 1fr` (Sidebar + main), Topbar sticky 56px avec breadcrumb gauche + search 280px droite, et le routing React Router 6 vers 5 modules (Dashboard, Etablissement, Travailleurs, Appareils, Actions) — pour l'instant chaque route rend un placeholder `<div>Module X</div>`. Wrapper TanStack Query Provider doit etre actif.\n\nAvant de coder, INSPECTE `design-mockup/project/src/*.jsx` pour les classes exactes (paddings, structures) et `src/components/ui/` pour reutiliser les primitifs (notamment les icones de `icons.tsx`). Ne pas reinventer un design parallele.\n\n## SCOPE STRICT\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src\\components\\layout\\Sidebar.tsx`\n- `...\\src\\components\\layout\\Topbar.tsx`\n- `...\\src\\components\\layout\\AppShell.tsx`\n- `...\\src\\App.tsx` (EDITER: remplacer le placeholder par le routing)\n- `...\\src\\main.tsx` (EDITER UNIQUEMENT pour wrapper QueryClientProvider et BrowserRouter — sinon ne pas toucher)\n- `...\\src\\modules\\dashboard\\Dashboard.tsx` (CREER placeholder `<div>Dashboard</div>`)\n- `...\\src\\modules\\etablissement\\Etablissement.tsx` (CREER placeholder)\n- `...\\src\\modules\\travailleurs\\TravailleursList.tsx` (CREER placeholder)\n- `...\\src\\modules\\appareils\\AppareilsList.tsx` (CREER placeholder)\n- `...\\src\\modules\\actions\\Actions.tsx` (CREER placeholder)\n\nINTERDIT: modifier les composants UI primitifs, le CSS, Tailwind config, ou tout fichier Rust. Pas de logique metier dans cette tache — uniquement la coque + routing.\n\n## Edits attendus\n1. `Sidebar.tsx`: composant sticky `h-screen w-60 bg-surface border-r border-border` + brand en haut (`PCR Manager` 18px padding 16px). Liste `NavLink` (react-router-dom) pointant vers `/`, `/etablissement`, `/travailleurs`, `/appareils`, `/actions`. Items: `flex items-center gap-2.5 px-2.5 py-1.5 rounded text-sm text-textMuted hover:bg-surfaceHover`. Etat actif (via `NavLink` className function): `bg-accentSoft text-accent border border-accentSoftBorder`. Badge count optionnel a droite (typage `count?: number`). Utiliser icones `LayoutDashboard`, `Building2`, `Users`, `Wrench`, `ListChecks` depuis `src/components/icons.tsx`.\n2. `Topbar.tsx`: barre `sticky top-0 z-10 h-14 bg-surface border-b border-border flex items-center justify-between px-6`. Breadcrumb gauche (props `breadcrumb: { label: string; to?: string }[]`). Search box droite: 280px width, icone `Search` a gauche, input `placeholder=Rechercher...`. Avatar a droite du search (initiale dans un cercle 32px bg-accentSoft text-accent).\n3. `AppShell.tsx`: layout `grid grid-cols-[240px_1fr] h-screen`. Slot enfant via `<Outlet />` (react-router-dom v6). `<main className=\"overflow-auto\"><Topbar .../><div className=\"px-8 pt-7 pb-14 max-w-[1320px] mx-auto\"><Outlet/></div></main>`.\n4. `App.tsx`: definir routing\n   ```tsx\n   <Routes>\n     <Route element={<AppShell/>}>\n       <Route index element={<Dashboard/>}/>\n       <Route path=\"etablissement\" element={<Etablissement/>}/>\n       <Route path=\"travailleurs\" element={<TravailleursList/>}/>\n       <Route path=\"appareils\" element={<AppareilsList/>}/>\n       <Route path=\"actions\" element={<Actions/>}/>\n     </Route>\n   </Routes>\n   ```\n5. `main.tsx`: importer `BrowserRouter` (de `react-router-dom`) et `QueryClient`/`QueryClientProvider` (de `@tanstack/react-query`). Wrapper:\n   ```tsx\n   <QueryClientProvider client={queryClient}>\n     <BrowserRouter>\n       <App/>\n     </BrowserRouter>\n   </QueryClientProvider>\n   ```\n   Conserver `<React.StrictMode>` et l'import de `./index.css` deja presents.\n6. Les 5 placeholders modules: simplement `export default function NomModule() { return <div>NomModule</div>; }`.\n\n## Critere d'acceptation\n- `npx tsc --noEmit` passe.\n- Les 5 routes sont declarees et resolvent leur placeholder.\n- Sidebar et Topbar utilisent uniquement classes Tailwind mappees tokens (aucun hex en dur).\n\n## Commande de validation\n```powershell\nnpx tsc --noEmit\n```\n\n## Rapport final (3 lignes max)\nLigne 1: fichiers livres. Ligne 2: resultat `tsc`. Ligne 3: anomalies hors-scope NON corrigees."
  },
  {
    "id": 8,
    "titre": "Implementer lib status habilitation et wrapper invoke",
    "prompt": "## Contexte\nRepo: `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39`. Tu dois fournir 3 modules TS:\n- `src/lib/api.ts`: wrapper typé sur `invoke` de `@tauri-apps/api/core` pour appeler les commandes Rust (signatures connues uniquement via `generate_handler!` declare dans `src-tauri/src/lib.rs`).\n- `src/lib/status.ts`: helpers pour calculer un statut couleur (`'valide'|'a_prevoir'|'en_retard'|'non_applicable'`) a partir d'une date d'echeance et d'un seuil d'alerte en mois.\n- `src/lib/habilitation.ts`: helper TS qui mappe le resultat de la commande Rust `habilitation_compute` vers un objet d'affichage (libelle + variante de badge).\n\nAvant d'ecrire `api.ts`, LIRE `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src-tauri\\src\\lib.rs` et chaque fichier sous `src-tauri/src/commands/*.rs` pour relever les NOMS exacts des commandes et leurs signatures. Aucune commande inventee. Si une commande listee dans le brief n'existe pas, RAPPORTER sans la creer.\n\nLire egalement `src-tauri/src/models.rs` pour relever les noms exacts des champs de chaque struct (utiliser le SAME nom serde — par defaut snake_case Rust).\n\n## SCOPE STRICT\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src\\lib\\api.ts` (CREER)\n- `...\\src\\lib\\status.ts` (CREER)\n- `...\\src\\lib\\habilitation.ts` (CREER)\n- `...\\src\\types\\domain.ts` (CREER — types TS miroir minimal des structs Rust effectivement utilisees)\n\nINTERDIT: modifier le code Rust, les composants UI, le routing, ou tout autre fichier.\n\n## Edits attendus\n1. `types/domain.ts`: pour chaque struct lue dans `models.rs`, declarer un type TS equivalent (champs en snake_case identique a serde par defaut). Pour `HabilitationStatus`, definir `type HabilitationStatut = 'validee' | 'partielle' | 'non_validee'` et la struct details.\n2. `lib/api.ts`: une fonction wrapper par commande Rust, typee. Exemple:\n   ```ts\n   import { invoke } from '@tauri-apps/api/core';\n   import type { Etablissement } from '../types/domain';\n   export const api = {\n     etablissement: {\n       list: () => invoke<Etablissement[]>('etablissement_list'),\n       get: (id: number) => invoke<Etablissement>('etablissement_get', { id }),\n       create: (input: Omit<Etablissement,'id'>) => invoke<Etablissement>('etablissement_create', { input }),\n       update: (input: Etablissement) => invoke<Etablissement>('etablissement_update', { input }),\n       delete: (id: number) => invoke<void>('etablissement_delete', { id }),\n     },\n     // ... idem pour les autres modules\n   };\n   ```\n   Couvrir TOUS les modules detectes dans `commands/*.rs` (etablissement, travailleur, habilitation, appareil, competence, verification, controle_qualite, document) plus passkey si present.\n3. `lib/status.ts`:\n   ```ts\n   export type StatusColor = 'valide' | 'a_prevoir' | 'en_retard' | 'non_applicable';\n   export function statusFromDate(deadlineIso: string | null | undefined, alertMonths: number = 1): StatusColor {\n     if (!deadlineIso) return 'non_applicable';\n     const now = new Date();\n     const deadline = new Date(deadlineIso);\n     if (Number.isNaN(deadline.getTime())) return 'non_applicable';\n     if (deadline < now) return 'en_retard';\n     const alertThreshold = new Date(now); alertThreshold.setMonth(alertThreshold.getMonth() + alertMonths);\n     if (deadline <= alertThreshold) return 'a_prevoir';\n     return 'valide';\n   }\n   export const statusToBadgeVariant: Record<StatusColor, 'ok'|'warn'|'danger'|'neutral'> = {\n     valide: 'ok', a_prevoir: 'warn', en_retard: 'danger', non_applicable: 'neutral',\n   };\n   ```\n4. `lib/habilitation.ts`:\n   ```ts\n   import type { HabilitationStatut } from '../types/domain';\n   export const habilitationToBadge: Record<HabilitationStatut, { label: string; variant: 'ok'|'warn'|'neutral' }> = {\n     validee: { label: 'Validee', variant: 'ok' },\n     partielle: { label: 'Partielle', variant: 'warn' },\n     non_validee: { label: 'Non validee', variant: 'neutral' },\n   };\n   ```\n\n## Critere d'acceptation\n- `npx tsc --noEmit` passe.\n- `api.ts` couvre toutes les commandes effectivement declarees dans le `generate_handler!` Rust (verifie par lecture, pas par invention).\n- Tous les noms de parametres passes a `invoke` correspondent EXACTEMENT au nommage attendu par Rust (camelCase si Tauri serialise ainsi ses parametres). Tauri 2 par defaut convertit snake_case Rust en camelCase JS pour les arguments — appliquer cette convention.\n\n## Commande de validation\n```powershell\nnpx tsc --noEmit\n```\n\n## Rapport final (3 lignes max)\nLigne 1: nombre de commandes wrappees + modules. Ligne 2: resultat `tsc`. Ligne 3: ecarts entre brief et code Rust reel RAPPORTES sans correction."
  },
  {
    "id": 9,
    "titre": "Implementer le module Dashboard avec KPI et alertes",
    "prompt": "## Contexte\nRepo: `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39`. Tu dois remplacer le placeholder `src/modules/dashboard/Dashboard.tsx` par la vue complete: grid KPI (3 colonnes) + cartes d'alertes par categorie (formations, dosimetries, verifications, controles qualite, visites medicales) avec statuts ok/warn/danger.\n\nAvant de coder, INSPECTE `design-mockup/project/src/*.jsx` (chercher `Dashboard` ou `KPI`) et `design-mockup/project/index.html` pour le placement, paddings, et libelles exacts. Reutiliser EXCLUSIVEMENT les composants UI deja crees (`KpiTile`, `Card`, `Badge`, `Dot`, `Button`) et les helpers de `src/lib/status.ts`.\n\nLes donnees viennent du wrapper `src/lib/api.ts` deja en place. Si une commande necessaire manque cote Rust, RAPPORTER sans la creer.\n\n## SCOPE STRICT\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src\\modules\\dashboard\\Dashboard.tsx` (REECRIRE le placeholder)\n- `...\\src\\modules\\dashboard\\AlertesCard.tsx` (CREER sous-composant carte d'alertes par categorie)\n\nINTERDIT: modifier les autres modules, les composants UI, les libs, le Rust. Pas de creation de nouvelles commandes Rust.\n\n## Edits attendus\n1. `Dashboard.tsx`:\n   - Hooks TanStack Query: `useQuery({ queryKey: ['etablissement'], queryFn: api.etablissement.get }); useQuery({ queryKey: ['travailleurs'], queryFn: api.travailleur.list }); useQuery({ queryKey: ['appareils'], queryFn: api.appareil.list });` (et autres en fonction des commandes effectivement disponibles).\n   - Grid KPI 3 cols: `Travailleurs actifs`, `Appareils suivis`, `Alertes en retard`. Chaque KPI utilise `<KpiTile label=... value=... footer=...>`.\n   - Section `<Card>` par categorie d'alerte: titre + table compacte des 5 prochaines echeances. Utiliser `statusFromDate()` + `statusToBadgeVariant` pour determiner la variante du `<Badge>`.\n   - Si pas de donnees (queries en loading ou empty), afficher un skeleton sobre (`<div className=\"text-textMuted text-sm\">Chargement...</div>` ou message vide).\n2. `AlertesCard.tsx`: props `{ titre: string; icone: React.ReactNode; alertes: { libelle: string; deadline: string | null; lienId?: number }[]; alertMonths?: number }`. Render `<Card><CardHead><div className=\"flex items-center gap-2\">{icone}<span>{titre}</span></div></CardHead><CardBody>...</CardBody></Card>`. Liste max 5 items, chaque ligne `flex items-center justify-between py-1.5 border-b border-border last:border-b-0` avec libelle a gauche et `<Badge variant={...}>{deadline formate FR ou 'N/A'}</Badge>` a droite.\n\n## Critere d'acceptation\n- `npx tsc --noEmit` passe.\n- Le Dashboard rend sans erreur runtime (verification structurelle uniquement — pas d'exec).\n- Aucun couleur en hex direct (tokens Tailwind uniquement).\n\n## Commande de validation\n```powershell\nnpx tsc --noEmit\n```\n\n## Rapport final (3 lignes max)\nLigne 1: composants livres. Ligne 2: resultat `tsc`. Ligne 3: commandes Rust manquantes detectees mais NON creees."
  },
  {
    "id": 10,
    "titre": "Implementer module Etablissement avec section K-Bis",
    "prompt": "## Contexte\nRepo: `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39`. Tu dois reecrire le placeholder `src/modules/etablissement/Etablissement.tsx` pour offrir un formulaire admin complet de l'etablissement (champs miroir de la table `etablissement` du schema) + une section K-Bis distincte (SIRET + upload PDF via la commande `document_upload`).\n\nAvant de coder, LIRE:\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src\\types\\domain.ts` pour la struct `Etablissement` exacte (champs).\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src\\lib\\api.ts` pour la signature exacte des commandes (`etablissement.get/create/update`, `document.upload`).\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\design-mockup\\project\\src\\*.jsx` (recherche `Etablissement` ou `KBis`) pour le layout exact.\n\nN'utiliser que des champs presents dans `domain.ts`. Pas d'invention.\n\n## SCOPE STRICT\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src\\modules\\etablissement\\Etablissement.tsx` (REECRIRE)\n- `...\\src\\modules\\etablissement\\KbisSection.tsx` (CREER)\n\nINTERDIT: modifier autres modules, libs, UI primitifs, code Rust. Pas de creation de commande Rust supplementaire.\n\n## Edits attendus\n1. `Etablissement.tsx`:\n   - `useQuery(['etablissement'], api.etablissement.get)` ; en attente: loader sobre.\n   - Formulaire compose de `<FormField.Field>` reutilisables, organise en sections logiques (Identification, Adresse, Contact PCR, etc.) selon les champs reels lus dans `domain.ts`.\n   - Bouton `Enregistrer` (variant primary) en haut a droite + bouton `Annuler` (ghost). Sur submit: `useMutation` => `api.etablissement.update`, invalide la query `['etablissement']`.\n   - Inclure `<KbisSection etablissement={data}/>` en bas.\n2. `KbisSection.tsx`:\n   - `<Card>` titre `Document K-Bis`.\n   - Affichage du SIRET en lecture seule (champ de l'etablissement) + champ editable si non rempli.\n   - Zone upload: input `type=\"file\" accept=\"application/pdf\"`. Au changement: lire le fichier via dialog Tauri (`@tauri-apps/plugin-dialog` si dispo, sinon utiliser `<input type=file>` natif), recuperer le chemin, appeler `api.document.upload({ entite_type: 'etablissement', entite_id: data.id, source_path })`. Apres succes, afficher le nom du fichier et un bouton `Telecharger` (placeholder qui appelle `api.document.get`).\n   - Si la commande de dialogue n'est pas presente dans `api.ts`, utiliser uniquement l'input file natif et passer le chemin disponible (sur Tauri, l'input file renvoie `File` mais pas le chemin systeme — dans ce cas, accepter `File` et envoyer son nom en attendant une commande backend appropriee, et RAPPORTER cette limitation).\n   - Validation: SIRET doit faire 14 chiffres ; afficher erreur en `text-danger` sinon.\n\n## Critere d'acceptation\n- `npx tsc --noEmit` passe.\n- Aucun champ inexistant dans `Etablissement` n'est reference.\n- Le composant utilise uniquement les composants UI primitifs (Button, Card, FormField, Badge si statut, etc.).\n\n## Commande de validation\n```powershell\nnpx tsc --noEmit\n```\n\n## Rapport final (3 lignes max)\nLigne 1: champs effectivement edites + composants livres. Ligne 2: resultat `tsc`. Ligne 3: limitations rencontrees (ex. dialog Tauri absent) RAPPORTEES sans correction."
  },
  {
    "id": 11,
    "titre": "Implementer module Travailleurs liste fiche et onglets",
    "prompt": "## Contexte\nRepo: `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39`. Tu dois construire le module Travailleurs complet:\n- Liste filtrable (`TravailleursList.tsx` — placeholder a reecrire)\n- Fiche detail avec 2 onglets `Donnees personnelles` et `Habilitation` (`TravailleurFiche.tsx`)\n- Contenu des 2 onglets dans des composants distincts (`DonneesPersonnellesTab.tsx`, `HabilitationTab.tsx`)\n- Sous-fiche Competences par appareil (`CompetencesAppareilSubsheet.tsx`)\n\nAvant de coder, LIRE:\n- `src/types/domain.ts` (struct `Travailleur`, `Habilitation`, `CompetenceTravailleur`, `CompetenceRef`)\n- `src/lib/api.ts` (commandes `travailleur.*`, `habilitation_compute`, `competence.*`)\n- `src/lib/habilitation.ts` (mapping badge)\n- `design-mockup/project/src/*.jsx` (recherche `Travailleur`) pour le layout.\n\nAjouter une route enfant `/travailleurs/:id` dans `App.tsx` SEULEMENT SI le routing actuel ne la possede pas — l'ajout exact a faire est decrit ci-dessous.\n\n## SCOPE STRICT\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src\\modules\\travailleurs\\TravailleursList.tsx` (REECRIRE)\n- `...\\src\\modules\\travailleurs\\TravailleurFiche.tsx` (CREER)\n- `...\\src\\modules\\travailleurs\\DonneesPersonnellesTab.tsx` (CREER)\n- `...\\src\\modules\\travailleurs\\HabilitationTab.tsx` (CREER)\n- `...\\src\\modules\\travailleurs\\CompetencesAppareilSubsheet.tsx` (CREER)\n- `...\\src\\App.tsx` (EDITER uniquement pour ajouter `<Route path=\"travailleurs/:id\" element={<TravailleurFiche/>}/>` enfant de `AppShell` — ne PAS toucher au reste)\n\nINTERDIT: modifier autres modules, UI primitifs, libs, code Rust. Pas de creation de commande Rust.\n\n## Edits attendus\n1. `TravailleursList.tsx`:\n   - `useQuery(['travailleurs'], api.travailleur.list)`\n   - Topbar de la page: bouton `Nouveau travailleur` (variant primary) + champ recherche local (filtrage cote client sur nom/prenom).\n   - `<Table>` colonnes (depend des champs reels de `Travailleur`): `Nom`, `Prenom`, `Fonction`, `Statut habilitation` (badge via mapping), `Actions` (icone `Pencil` pour edit). Click sur une ligne => navigate `/travailleurs/${id}`.\n2. `TravailleurFiche.tsx`:\n   - `useParams<{ id: string }>()` -> id numerique.\n   - `useQuery(['travailleur', id], () => api.travailleur.get(Number(id)))`\n   - Header avec nom + bouton `Retour` (ghost) + `Enregistrer`.\n   - `<Tabs>` avec `TabList` (`Donnees personnelles` / `Habilitation`) + `TabPanel` rendant respectivement `<DonneesPersonnellesTab/>` ou `<HabilitationTab/>`. Etat actif gere via `useState`.\n3. `DonneesPersonnellesTab.tsx`: formulaire (un `FormField.Field` par champ de `Travailleur` selon `domain.ts`). useMutation -> `api.travailleur.update`.\n4. `HabilitationTab.tsx`:\n   - `useQuery(['habilitation', id], () => api.habilitation.compute(id))`\n   - Affiche un grand badge en haut via `habilitationToBadge[data.statut]`.\n   - 4 sous-sections (formation RP, dosimetries, competences, visite med) chacune avec un `<Dot variant=...>` indiquant l'etat (depuis `data.details`).\n   - Section Competences: pour chaque appareil lie au travailleur, rendre `<CompetencesAppareilSubsheet appareilId=... travailleurId=...>`.\n5. `CompetencesAppareilSubsheet.tsx`:\n   - Props `{ appareilId: number; travailleurId: number }`.\n   - `useQuery` sur `api.competence.get_for_travailleur` (ou nom equivalent reel — verifier api.ts).\n   - Liste les 9 competences de reference (depuis `api.competence.list`) sous forme de table 2 colonnes (Libelle / Statut). Statut: `validee_seule` / `validee_complete` / `non_validee`. Bouton `Editer` pour cycle l'etat — utilise `api.competence.set`.\n\n## Critere d'acceptation\n- `npx tsc --noEmit` passe.\n- La route `/travailleurs/:id` est navigable depuis la liste.\n- Aucun champ ou commande inventee.\n\n## Commande de validation\n```powershell\nnpx tsc --noEmit\n```\n\n## Rapport final (3 lignes max)\nLigne 1: fichiers livres + route ajoutee. Ligne 2: resultat `tsc`. Ligne 3: limitations RAPPORTEES sans correction."
  },
  {
    "id": 12,
    "titre": "Implementer module Appareils fiche verifications et CQ",
    "prompt": "## Contexte\nRepo: `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39`. Tu dois construire le module Appareils:\n- Liste filtrable (`AppareilsList.tsx` — placeholder a reecrire).\n- Fiche detail avec sections Info + Caracteristiques + Verifications techniques (annuelle interne + triennale externe) + Controles Qualite (externe + 3 internes auto-generes).\n\nAvant de coder, LIRE:\n- `src/types/domain.ts` (struct `Appareil`, `VerificationTechnique`, `ControleQualite`)\n- `src/lib/api.ts` (commandes correspondantes)\n- `src/lib/status.ts` (helpers statut couleur)\n- `design-mockup/project/src/*.jsx` (recherche `Appareil`) pour le layout.\n\nAjouter la route `/appareils/:id` dans `App.tsx` UNIQUEMENT pour la fiche.\n\n## SCOPE STRICT\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src\\modules\\appareils\\AppareilsList.tsx` (REECRIRE)\n- `...\\src\\modules\\appareils\\AppareilFiche.tsx` (CREER)\n- `...\\src\\modules\\appareils\\VerificationsSection.tsx` (CREER)\n- `...\\src\\modules\\appareils\\ControlesQualiteSection.tsx` (CREER)\n- `...\\src\\App.tsx` (EDITER pour ajouter `<Route path=\"appareils/:id\" element={<AppareilFiche/>}/>` enfant de `AppShell`)\n\nINTERDIT: tout autre fichier. Pas de creation de commande Rust.\n\n## Edits attendus\n1. `AppareilsList.tsx`:\n   - `useQuery(['appareils'], api.appareil.list)`\n   - Bouton `Nouvel appareil` (primary), recherche locale.\n   - `<Table>`: colonnes selon les champs reels (`numero_serie`, `marque`, `modele`, `categorie`, `statut_verif` via `statusFromDate(prochaine_verif_date, 1)`, badge couleur).\n   - Click ligne => `/appareils/:id`.\n2. `AppareilFiche.tsx`:\n   - `useParams` -> id.\n   - Header bouton `Retour` + `Enregistrer`.\n   - Sections: Info generales (FormField), Caracteristiques, `<VerificationsSection appareilId/>`, `<ControlesQualiteSection appareilId/>`.\n3. `VerificationsSection.tsx`:\n   - Props `{ appareilId: number }`.\n   - `useQuery` -> liste verifications via `api.verification.list_for_appareil` (ou nom equivalent reel).\n   - Table 2 lignes: annuelle interne, triennale externe. Chaque ligne: derniere date + prochaine echeance + badge statut (`statusFromDate(prochaine, 1)`).\n   - Bouton `Saisir nouvelle verification` ouvre une modale (composant inline simple — div absolute + backdrop) avec formulaire date + type + observations.\n4. `ControlesQualiteSection.tsx`:\n   - Props `{ appareilId: number }`.\n   - `useQuery` -> liste CQ via `api.controle_qualite.list_for_appareil`.\n   - Affichage timeline simple: bullet externe puis 3 internes (partiel/complet/partiel) avec leurs dates et statuts couleur.\n   - Bouton `Saisir CQ externe` => modale similaire. Apres insert le trigger DB doit auto-creer les 3 internes (deja code SQL — pas a refaire ici). Bouton `Marquer CQ comme effectue` pour un CQ interne en attente.\n\n## Critere d'acceptation\n- `npx tsc --noEmit` passe.\n- Routes `/appareils` et `/appareils/:id` operationnelles.\n- Aucun champ ou commande inventee.\n\n## Commande de validation\n```powershell\nnpx tsc --noEmit\n```\n\n## Rapport final (3 lignes max)\nLigne 1: fichiers livres + route ajoutee. Ligne 2: resultat `tsc`. Ligne 3: limitations RAPPORTEES sans correction."
  },
  {
    "id": 13,
    "titre": "Implementer module Actions vue consolidee filtrable",
    "prompt": "## Contexte\nRepo: `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39`. Tu dois reecrire le placeholder `src/modules/actions/Actions.tsx` pour offrir la vue consolidee de toutes les actions a faire, agregeant les echeances de toutes les sources (formations a renouveler, dosimetries, verifications appareils, controles qualite, visites medicales) avec un filtre segmenté.\n\nAvant de coder, LIRE:\n- `src/lib/api.ts` (toutes les commandes list disponibles)\n- `src/lib/status.ts` (`statusFromDate`)\n- `src/types/domain.ts` (structs concernees)\n- `design-mockup/project/src/*.jsx` (recherche `Actions`) pour le layout.\n\nLe filtre segmente utilise le composant `PillFilter` deja cree avec options: `tout`, `en_retard`, `a_venir`, `formation`, `controle`, `visite_med`.\n\n## SCOPE STRICT\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\\src\\modules\\actions\\Actions.tsx` (REECRIRE)\n\nINTERDIT: tout autre fichier. Pas d'ajout de commande Rust. Pas de nouveau composant UI primitif.\n\n## Edits attendus\n1. `Actions.tsx`:\n   - Etat local `useState` pour `filter: 'tout' | 'en_retard' | 'a_venir' | 'formation' | 'controle' | 'visite_med'`. Defaut `'tout'`.\n   - Plusieurs `useQuery` en parallele: liste travailleurs (pour formations et visites med), liste appareils (pour verifications), liste CQ.\n   - Construire un tableau unifie `Action[]` de type:\n     ```ts\n     type Action = { id: string; categorie: 'formation'|'dosimetrie'|'verification'|'controle'|'visite_med'; libelle: string; deadline: string | null; cible: { type: 'travailleur'|'appareil'; id: number; label: string } };\n     ```\n     en transformant chaque ligne source. Pour les formations RP travailleurs: lire `date_formation_rp` et calculer deadline = date + 3 ans (sauf si fonction RP patient => +7 ans, selon flag dispo). Pour les verifications: utiliser `prochaine_verif_date` deja calculee cote DB si exposee, sinon recalculer.\n   - Filtrage cote client selon `filter`: `en_retard` => `statusFromDate(deadline) === 'en_retard'`; `a_venir` => `'a_prevoir'`; `formation`, `controle`, `visite_med` => filtre par categorie. `tout` => tout.\n   - Tri par deadline ascendant (en_retard d'abord).\n   - Rendu: `<PillFilter options={[...]} value={filter} onChange={setFilter}/>` puis `<Table>` colonnes `Categorie`, `Libelle`, `Cible`, `Echeance` (badge `statusFromDate`), `Action` (bouton ghost `Voir` => navigate vers fiche travailleur/appareil).\n   - Empty state: `<div className=\"text-textMuted text-sm py-8 text-center\">Aucune action.</div>`.\n\n## Critere d'acceptation\n- `npx tsc --noEmit` passe.\n- Toutes les sources d'echeance accessibles via `api.ts` sont integrees. Si une source manque (ex. pas de commande dosimetries cote Rust), RAPPORTER sans creer.\n- `PillFilter` est utilise correctement avec les 6 options ci-dessus.\n\n## Commande de validation\n```powershell\nnpx tsc --noEmit\n```\n\n## Rapport final (3 lignes max)\nLigne 1: nombre de sources integrees + total options filtre. Ligne 2: resultat `tsc`. Ligne 3: sources manquantes RAPPORTEES sans correction."
  },
  {
    "id": 14,
    "titre": "Verifier build complet TypeScript Rust et Vite",
    "prompt": "## Contexte\nRepo: `C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39`. Cette tache finale execute la verification complete de l'application sans rien creer ni modifier (sauf si une correction triviale d'erreur de compilation s'avere imperative — auquel cas elle reste contenue au strict minimum).\n\nObjectif: lancer 3 verifications:\n1. `npx tsc --noEmit` (types TS)\n2. `cargo check --manifest-path src-tauri/Cargo.toml` (Rust)\n3. `npx vite build` (build frontend uniquement; sortie dans `dist/`)\n\nNE PAS executer `npm run tauri build` ni `tauri dev` (long, exigent NSIS et installation native).\n\n## SCOPE STRICT\nAucune creation de fichier. AUCUNE modification de fichier sauf en derniere extremite et UNIQUEMENT pour corriger une erreur de compilation triviale (typo, import manquant) en restant dans le fichier qui leve l'erreur. Toute autre erreur doit etre RAPPORTEE sans correction.\n\nINTERDIT absolu: refactor, renommage, optimisation, ajout de feature, suppression de fichier, modification de `.gitignore`, edition de la configuration Tauri, edition des migrations SQL, edition du `package.json` (sauf si npm exige une dependance manquante triviale revelee par tsc).\n\n## Edits attendus\nIDEAL: zero edit. Si echec d'un build:\n- TS: identifier les 3 premieres erreurs et RAPPORTER (sans corriger sauf si trivial, ex. import manquant sur 1 ligne).\n- Cargo: idem.\n- Vite: idem.\n\nNE PAS tuer de processus ni utiliser `taskkill /F /IM node.exe` (cela tuerait les autres sessions Claude Code en cours). Si un port est bloque (1420), simplement RAPPORTER — `npx vite build` n'utilise pas le port dev.\n\n## Commandes a executer (dans cet ordre, depuis la racine du repo)\n```powershell\ncd \"C:\\work\\PCR Manager\\.claude\\worktrees\\dreamy-villani-248f39\"\nnpx tsc --noEmit\ncargo check --manifest-path \"src-tauri\\Cargo.toml\"\nnpx vite build\n```\n\n## Critere d'acceptation\n- Les 3 commandes ci-dessus s'executent et leur code retour est connu.\n- Pour chaque echec, le 1er bloc d'erreur est cite (max 10 lignes).\n- Aucune modification hors-scope effectuee.\n\n## Commande de validation finale (synthese)\n```powershell\nnpx tsc --noEmit ; cargo check --manifest-path \"src-tauri\\Cargo.toml\" ; npx vite build\n```\n\n## Rapport final (3 lignes max)\nLigne 1: tsc => OK ou N erreurs (1er message). Ligne 2: cargo => OK ou N erreurs (1er message). Ligne 3: vite build => OK ou 1er message + anomalies hors-scope NON corrigees."
  }
]