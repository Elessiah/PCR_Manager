[
  {
    "id": 1,
    "titre": "Ajouter .editorconfig et .gitattributes a la racine",
    "executor": "qwen",
    "executor_reason": "Creation mecanique de 2 fichiers de config avec contenu fourni mot pour mot, aucun jugement requis.",
    "prompt": "Cree deux fichiers de configuration a la racine du repo.\n\nREPO ROOT : `C:\\work\\PCR Manager\\.claude\\worktrees\\compassionate-wilbur-9861c8`\n\nSCOPE STRICT (seules modifications autorisees) :\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\compassionate-wilbur-9861c8\\.editorconfig` (CREATION)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\compassionate-wilbur-9861c8\\.gitattributes` (CREATION)\n\nToute modification hors de cette liste est INTERDITE. Si tu reperes un bug hors-scope, RAPPORTE-le sans le corriger.\n\nINTERDIT : creer des helper scripts ad-hoc (`*.sh`, `*.ps1`, `*.bat`, `*.cmd`, `validate-*`, `run-*`, `test-runner*`, `check-*`). Les commandes de validation s'executent directement via Bash.\n\nPREREQUIS : Avant de creer chaque fichier, lance `Test-Path` (PowerShell) ou equivalent pour verifier qu'il n'existe pas deja. S'il existe deja, LIS-le d'abord et ne l'ecrase QUE si son contenu actuel ne correspond pas a celui requis.\n\nCONTENU EXACT de `.editorconfig` :\n```\nroot = true\n\n[*]\nindent_style = space\nindent_size = 2\nend_of_line = lf\ncharset = utf-8\ntrim_trailing_whitespace = true\ninsert_final_newline = true\n\n[*.md]\ntrim_trailing_whitespace = false\n\n[Makefile]\nindent_style = tab\n```\n\nCONTENU EXACT de `.gitattributes` :\n```\n# Auto-normalize line endings\n* text=auto eol=lf\n\n# Force LF for specific types\n*.ts text eol=lf\n*.tsx text eol=lf\n*.rs text eol=lf\n*.sql text eol=lf\n*.json text eol=lf\n*.md text eol=lf\n*.toml text eol=lf\n*.css text eol=lf\n*.html text eol=lf\n\n# Binary files — don't touch\n*.png binary\n*.ico binary\n*.exe binary\n*.dll binary\n```\n\nTESTS ASSOCIES : Exception toleree — ce sont des fichiers de CONFIGURATION PURE non testables unitairement. Aucun test a ajouter.\n\nCRITERE D'ACCEPTATION :\n- Les deux fichiers existent et contiennent exactement le contenu ci-dessus.\n- `git status --short` montre les deux fichiers comme ajoutes.\n\nCOMMANDE DE VALIDATION (executer depuis le repo root) :\n```\ngit status --short\n```\nLa sortie doit lister `?? .editorconfig` et `?? .gitattributes` (et rien d'autre de pertinent au scope).\n\nCOMMIT GIT (OBLIGATOIRE, apres validation OK) :\n```\ngit status --short\ngit add .editorconfig .gitattributes\ngit commit -m \"chore(repo): ajouter editorconfig et gitattributes\"\n```\nAUCUN trailer `Co-Authored-By`, AUCUN `--amend`, AUCUN `--no-verify`.\nCite le SHA court (7 chars) du commit dans le rapport final.\n\nRAPPORT FINAL (3 lignes max) :\n- Ligne 1 : fichiers crees\n- Ligne 2 : resultat validation\n- Ligne 3 : SHA court du commit"
  },
  {
    "id": 2,
    "titre": "Auditer et corriger patterns React conditionnels avec entiers",
    "executor": "haiku",
    "executor_reason": "Necessite de juger si chaque LHS est un entier potentiel ou un booleen, exploration multi-fichiers avec decisions contextuelles.",
    "prompt": "Audit React : trouver et corriger tous les patterns `{x && <JSX>}` ou `x` pourrait etre un entier (rendrait `\"0\"` dans le DOM lorsque la valeur est 0).\n\nREPO ROOT : `C:\\work\\PCR Manager\\.claude\\worktrees\\compassionate-wilbur-9861c8`\n\nSCOPE STRICT (seules modifications autorisees) :\n- Tout fichier sous `src\\modules\\`, `src\\components\\`, `src\\lib\\` (extensions `.ts`, `.tsx`) qui contient un pattern a corriger.\n- AUCUN autre fichier (pas de tests, pas de config, pas de docs).\n\nToute modification hors de cette liste est INTERDITE. Si tu reperes un bug hors-scope, RAPPORTE-le sans le corriger.\n\nINTERDIT : creer des helper scripts ad-hoc (`*.sh`, `*.ps1`, `*.bat`, `*.cmd`, `validate-*`, `run-*`, `test-runner*`, `check-*`). Toutes les commandes via Bash tool.\n\nPROCEDURE :\n1. Recherche initiale via Grep tool (pas via Bash `grep`) :\n   - pattern : `\\{[a-zA-Z_][a-zA-Z0-9_.]* && <`\n   - path : `src/modules`, puis `src/components`, puis `src/lib`\n   - output_mode : `content`, avec `-n`\n2. Pour CHAQUE match, LIS le fichier complet via Read tool pour comprendre le type du LHS :\n   - Examine les types TypeScript autour du symbole (interfaces, types des props, retour de hook).\n   - Si tu doutes du type d'un champ, LIS `src\\lib\\domain.ts` pour les types metier.\n3. Classement :\n   - LHS = entier potentiel (champ nomme `count`, `length`, `id`, `nombre`, `total`, ou typage `number`, ou champ booleen-like stocke en `INTEGER` SQLite type 0/1) → CORRIGER en `{Boolean(x) && <...>}` (preferer cette forme, simple et universelle).\n   - LHS = booleen natif explicite (`isLoading`, `isOpen`, `hasData`, retour `===`, retour `!!x`, type `boolean`) → LAISSER tel quel.\n   - LHS = string → LAISSER tel quel (string vide rend rien sauf cas exotiques ; ne pas toucher).\n4. Pour chaque correction, utilise Edit tool. Pas de replace_all (chaque cas est contextuel).\n5. Cas precedent connu deja corrige (a verifier toujours present, sinon RAPPORTER) : `src\\modules\\appareils\\AppareilsList.tsx` ligne avec `{a.utilisation_partagee && <Badge>}` → devrait deja etre `{Boolean(a.utilisation_partagee) && <Badge>}`. Si non, le corriger.\n\nVERIFICATION DES VERSIONS : pas de nouvelle dependance ajoutee dans cette tache. Avant tout doute sur un type React/lib, LIS `package.json` pour confirmer (React 18, TypeScript strict).\n\nTESTS ASSOCIES (OBLIGATOIRE) : aucune nouvelle fonction publique creee ici (correction in-place de JSX existant). Exception toleree : les tests existants des composants modifies (s'ils existent dans `*.test.tsx` voisins) DOIVENT continuer a passer. Si un composant modifie n'a PAS de test voisin, RAPPORTE-le dans le rapport final (ne pas creer de test dans cette tache — out of scope).\n\nCRITERE D'ACCEPTATION :\n- Tout pattern `{<int> && <JSX>}` repere dans le scope est corrige en `{Boolean(<int>) && <JSX>}` ou equivalent.\n- `npm run typecheck` passe.\n- `npm run test:run` passe (192/192 ou plus).\n- Le rapport final liste : nombre de matches inspectes, nombre corriges, nombre laisses (avec justification 1 ligne).\n\nCOMMANDE DE VALIDATION (depuis repo root) :\n```\nnpm run typecheck && npm run test:run\n```\nSortie attendue : 0 erreur TS, tous tests verts.\n\nCOMMIT GIT (apres validation OK) :\n```\ngit status --short\ngit add <chemins exacts des fichiers modifies>\ngit commit -m \"fix(react): corriger patterns conditionnels avec entiers potentiels\"\n```\nAUCUN trailer `Co-Authored-By`, AUCUN `--amend`, AUCUN `--no-verify`.\nCite le SHA court (7 chars) dans le rapport final. Si aucun pattern n'a du etre corrige, NE PAS faire de commit vide ; rapporter \"no changes\".\n\nRAPPORT FINAL (3 lignes max) :\n- Ligne 1 : N matches inspectes / M corriges / K laisses\n- Ligne 2 : resultat typecheck+tests\n- Ligne 3 : SHA court (ou \"no commit, no changes\")"
  },
  {
    "id": 3,
    "titre": "Creer CompetencesList.tsx avec test associe",
    "executor": "haiku",
    "executor_reason": "Nouveau composant complexe avec mutations React Query, modale, table triable et tests RTL ; demande du jugement sur la structure et l'integration.",
    "prompt": "Cree le composant page `CompetencesList` pour la gestion CRUD de la bibliotheque de competences, ainsi que son test associe.\n\nREPO ROOT : `C:\\work\\PCR Manager\\.claude\\worktrees\\compassionate-wilbur-9861c8`\n\nSCOPE STRICT (seules creations/modifications autorisees) :\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\compassionate-wilbur-9861c8\\src\\modules\\competences\\CompetencesList.tsx` (CREATION)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\compassionate-wilbur-9861c8\\src\\modules\\competences\\CompetencesList.test.tsx` (CREATION)\n\nToute autre modification est INTERDITE (la route et la sidebar sont traitees par d'autres taches). Si tu reperes un bug hors-scope, RAPPORTE-le sans corriger.\n\nINTERDIT : creer des helper scripts ad-hoc (`*.sh`, `*.ps1`, `*.bat`, `*.cmd`, `validate-*`, `run-*`, `test-runner*`, `check-*`). Toutes les commandes via Bash tool.\n\nVERIFICATION DES VERSIONS (OBLIGATOIRE AVANT DE CODER) :\n1. LIS `package.json` pour confirmer : React 18, @tanstack/react-query, vitest, @testing-library/react. Note la version exacte de react-query (v4 vs v5 a une API differente pour `useMutation`, `useQuery`, et `QueryClient`).\n2. NE PAS supposer l'API react-query : si version 5, les options sont `{ mutationFn, onSuccess }` ; si version 4, idem mais checks structurels differents. UTILISE l'API EXACTE de la version listee.\n3. NE PAS importer depuis `@testing-library/react-hooks` (deprecie) ; utilise `renderHook` de `@testing-library/react` si dispo en v13+, sinon mock direct.\n\nLECTURES PREREQUISES (avant d'ecrire la moindre ligne) :\n- `src\\lib\\api.ts` : verifier que `api.competence.list()`, `.create()`, `.update()`, `.delete()` existent ET la signature exacte (args, retours, async).\n- `src\\lib\\domain.ts` : verifier le type `CompetenceRef` (champs exacts : `id`, `libelle`, `ordre`).\n- `src\\components\\ui\\index.ts` (ou equivalent barrel) : verifier les exports disponibles. Liste attendue : `PageHead`, `Table`, `THead`, `TBody`, `TR`, `TH`, `TD`, `Button`, `Badge`, `Field`, `Label`, `Input`. Si un primitif manque, RAPPORTE-le et utilise ce qui est disponible.\n- `src\\modules\\travailleurs\\TravailleursList.tsx` (ou un autre module existant) : LIS-le pour copier le STYLE et la STRUCTURE (PageHead + tableau + actions). Reproduis la meme architecture, n'invente pas une nouvelle convention.\n- Un test voisin (par ex. `src\\modules\\travailleurs\\TravailleursList.test.tsx` s'il existe) pour copier le pattern de test (QueryClientProvider wrapper, mocks de api, render assertions).\n\nCOMPOSANT A CREER — `CompetencesList.tsx` :\n- Default export `function CompetencesList()`.\n- `useQuery({ queryKey: ['competences'], queryFn: () => api.competence.list() })`.\n- Etat local pour modale : `const [editing, setEditing] = useState<CompetenceRef | null>(null)` et `const [creating, setCreating] = useState(false)`.\n- PageHead avec titre `\"Bibliotheque de competences\"` (sans accent grave conserve les conventions du repo — si les autres PageHead utilisent des accents, fais comme eux).\n- Bouton primary `\"Ajouter une competence\"` qui ouvre la modale en mode creation.\n- Table avec colonnes : `Ordre`, `Libelle`, `Actions`. Lignes triees par `ordre` ascendant.\n- Pour chaque ligne, deux boutons ghost : `\"Editer\"` (ouvre modale en mode edition prefillee), `\"Supprimer\"` (variant destructive ; demande confirmation via `window.confirm` ou une seconde modale — preferer la seconde modale pour coherence, sinon `window.confirm` est acceptable et plus simple a tester).\n- Modale = `div` conditionnel dans le JSX (PAS de Portal). Structure : `<div className=\"fixed inset-0 bg-black/40 flex items-center justify-center z-50\">` + un panneau interne. Contient `Field/Label/Input` pour `libelle` et `ordre` (input number).\n- Mutations : `useMutation` pour create, update, delete. Chaque `onSuccess` doit appeler `queryClient.invalidateQueries({ queryKey: ['competences'] })` (syntaxe v5 ; adapte si v4).\n- Etat `isLoading` du query → afficher `\"Chargement...\"` ou skeleton selon convention du repo.\n- Etat `error` → afficher message d'erreur sobre.\n- AUCUN style inline, AUCUN composant tiers, UNIQUEMENT les primitifs UI du repo + classes Tailwind.\n- Imports : `@/components/ui` (alias). `@/lib/api`, `@/lib/domain`. PAS de `use client`.\n\nTESTS ASSOCIES — `CompetencesList.test.tsx` (OBLIGATOIRE) :\nCouvre AU MINIMUM chemin nominal + 1 cas limite par fonction publique. Suggestions concretes :\n1. Nominal `affiche la liste triee par ordre` : mock `api.competence.list` retournant 3 entrees avec ordre [2, 0, 1] ; render dans QueryClientProvider ; verifie que les lignes apparaissent dans l'ordre 0, 1, 2.\n2. Edge `liste vide` : mock retournant `[]` ; verifie qu'aucune ligne table n'est rendue (et idealement un message vide si convention repo).\n3. Nominal `clic Ajouter ouvre la modale` : render ; click sur bouton \"Ajouter\" ; verifie qu'un champ libelle est present.\n4. Edge `submit creation appelle api.competence.create` : remplit le formulaire ; submit ; verifie que le mock create a ete appele avec les bons args.\n5. Edge `clic Supprimer appelle api.competence.delete apres confirmation` : mock `window.confirm` a `true` ; click delete ; verifie l'appel.\n\nUtilise `vi.fn()` + `vi.mock('@/lib/api', ...)` selon le pattern des autres tests. Wrappe dans `QueryClientProvider` avec un `QueryClient` fresh par test (retry: false).\n\nCRITERE D'ACCEPTATION :\n- Les deux fichiers existent et sont fonctionnels.\n- `npm run typecheck` passe sans nouvelle erreur.\n- `npm run test:run` passe (toutes les suites incluant la nouvelle).\n- Au moins 5 cas de test pour CompetencesList passent.\n\nCOMMANDE DE VALIDATION (depuis repo root) :\n```\nnpm run typecheck && npm run test:run -- competences\n```\nPuis verification globale :\n```\nnpm run test:run\n```\n\nCOMMIT GIT (apres validation OK) :\n```\ngit status --short\ngit add src/modules/competences/CompetencesList.tsx src/modules/competences/CompetencesList.test.tsx\ngit commit -m \"feat(competences): ajouter UI CRUD bibliotheque de competences\"\n```\nAUCUN trailer `Co-Authored-By`, AUCUN `--amend`, AUCUN `--no-verify`.\nCite le SHA court dans le rapport final.\n\nRAPPORT FINAL (3 lignes max) :\n- Ligne 1 : composant + N cas de test crees\n- Ligne 2 : resultat typecheck + tests (chiffres exacts)\n- Ligne 3 : SHA court"
  },
  {
    "id": 4,
    "titre": "Enregistrer la route /competences dans App.tsx",
    "executor": "qwen",
    "executor_reason": "Ajout mecanique d'une `<Route>` selon le pattern existant deja present dans le fichier.",
    "prompt": "Ajoute la route `/competences` pointant vers le composant `CompetencesList` dans `App.tsx`.\n\nREPO ROOT : `C:\\work\\PCR Manager\\.claude\\worktrees\\compassionate-wilbur-9861c8`\n\nSCOPE STRICT (seules modifications autorisees) :\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\compassionate-wilbur-9861c8\\src\\App.tsx` (EDITION)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\compassionate-wilbur-9861c8\\src\\App.test.tsx` (EDITION SEULEMENT SI le fichier existe deja ; sinon NE PAS le creer)\n\nToute modification hors de cette liste est INTERDITE. Si tu reperes un bug hors-scope, RAPPORTE-le sans corriger.\n\nINTERDIT : creer des helper scripts ad-hoc (`*.sh`, `*.ps1`, `*.bat`, `*.cmd`, `validate-*`, `run-*`, `test-runner*`, `check-*`).\n\nLECTURE PREREQUISE OBLIGATOIRE :\n1. LIS `src\\App.tsx` en entier AVANT toute modification. Identifie :\n   - L'import existant pour `Route` et `Routes` (`react-router-dom`).\n   - La syntaxe des `<Route path=\"...\" element={...} />` deja presents.\n   - Le bloc d'imports en haut du fichier.\n2. LIS `package.json` pour confirmer la version de `react-router-dom` (v6 attendue ; si v7 voir migration). Utilise l'API EXACTE de la version installee.\n3. Verifie que le fichier `src\\modules\\competences\\CompetencesList.tsx` existe (cree par une tache anterieure). S'il n'existe pas, ARRETE et RAPPORTE — l'import echouera.\n\nEDIT PRECIS :\n1. Ajoute en haut du fichier (avec les autres imports de modules) :\n   ```\n   import CompetencesList from '@/modules/competences/CompetencesList';\n   ```\n   (Si les autres imports utilisent `./modules/...` au lieu de l'alias `@/modules/...`, COPIE LE PATTERN existant — ne mixe pas les conventions.)\n2. Dans le bloc `<Routes>`, ajoute une `<Route>` :\n   ```\n   <Route path=\"/competences\" element={<CompetencesList />} />\n   ```\n   Place-la a une position logique (entre travailleurs et actions si possible — sinon en suivant l'ordre existant).\n\nVERIFICATION DES VERSIONS : confirme via lecture de `package.json` que `react-router-dom` est en v6.x. Si v7, adapte la syntaxe (mais l'API `<Route path element>` reste compatible).\n\nTESTS ASSOCIES (OBLIGATOIRE) :\n- Si `src\\App.test.tsx` existe : ajoute UN cas de test verifiant que la route `/competences` rend `CompetencesList` (utilise `MemoryRouter` avec `initialEntries={['/competences']}` et un mock leger de CompetencesList si necessaire).\n- Si `src\\App.test.tsx` n'existe PAS : exception toleree (artefact d'enregistrement de routing, couvert indirectement par les tests du composant cible). RAPPORTE explicitement cette absence dans le rapport final.\n\nCRITERE D'ACCEPTATION :\n- `npm run typecheck` passe.\n- `npm run test:run` passe.\n- Le chemin `/competences` navigue vers CompetencesList (verifiable via le test si applicable).\n\nCOMMANDE DE VALIDATION (depuis repo root) :\n```\nnpm run typecheck && npm run test:run\n```\n\nCOMMIT GIT (apres validation OK) :\n```\ngit status --short\ngit add src/App.tsx\ngit add src/App.test.tsx   # SEULEMENT si modifie/cree\ngit commit -m \"feat(routes): enregistrer route competences\"\n```\nAUCUN trailer `Co-Authored-By`, AUCUN `--amend`, AUCUN `--no-verify`.\nCite le SHA court dans le rapport final.\n\nRAPPORT FINAL (3 lignes max) :\n- Ligne 1 : route ajoutee + test (oui/non, justification si non)\n- Ligne 2 : resultat validation\n- Ligne 3 : SHA court"
  },
  {
    "id": 5,
    "titre": "Ajouter entree Competences dans la Sidebar",
    "executor": "qwen",
    "executor_reason": "Ajout mecanique d'une entree de menu selon le pattern existant des autres entrees de la sidebar.",
    "prompt": "Ajoute une entree de navigation `Competences` dans la sidebar, avec l'icone `BookOpen` de `lucide-react`, positionnee entre `Travailleurs` et `Actions`.\n\nREPO ROOT : `C:\\work\\PCR Manager\\.claude\\worktrees\\compassionate-wilbur-9861c8`\n\nSCOPE STRICT (seules modifications autorisees) :\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\compassionate-wilbur-9861c8\\src\\components\\Sidebar.tsx` (EDITION)\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\compassionate-wilbur-9861c8\\src\\components\\Sidebar.test.tsx` (EDITION SEULEMENT SI le fichier existe deja)\n\nToute autre modification est INTERDITE. Si tu reperes un bug hors-scope, RAPPORTE-le sans corriger.\n\nINTERDIT : creer des helper scripts ad-hoc (`*.sh`, `*.ps1`, `*.bat`, `*.cmd`, `validate-*`, `run-*`, `test-runner*`, `check-*`).\n\nLECTURE PREREQUISE OBLIGATOIRE :\n1. LIS `src\\components\\Sidebar.tsx` en entier AVANT toute modification.\n2. Identifie la STRUCTURE existante : array d'items de navigation, ou liste statique JSX ? Composant `NavLink` ? Reproduit EXACTEMENT le meme pattern.\n3. Note les imports `lucide-react` deja presents : ajoute `BookOpen` a l'import existant (ne pas creer une seconde ligne d'import).\n4. Verifie `package.json` que `lucide-react` est bien installe (devrait l'etre — confirme l'icone `BookOpen` y existe ; sinon utilise une icone alternative comme `Library` ou `GraduationCap` et RAPPORTE le choix).\n\nEDIT PRECIS :\n1. Import : ajoute `BookOpen` au bloc d'import existant de `lucide-react`.\n2. Entree de menu : insere entre l'entree `Travailleurs` et `Actions` :\n   - Label : `\"Competences\"` (ou avec accent grave si le repo utilise des accents dans les labels — copie le style des autres labels en lisant les voisins).\n   - Path : `/competences`.\n   - Icon : `BookOpen`.\n   Reproduit EXACTEMENT la forme des autres entrees (que ce soit un objet dans un array, ou un `<NavLink>` JSX direct).\n\nVERIFICATION DES VERSIONS : `lucide-react` doit etre installe ; consulte `package.json` pour confirmer.\n\nTESTS ASSOCIES (OBLIGATOIRE) :\n- Si `src\\components\\Sidebar.test.tsx` existe : ajoute UN cas verifiant que le lien `Competences` est rendu et pointe vers `/competences` (ex : `expect(screen.getByRole('link', { name: /comp[ée]tences/i }))`).\n- Si le test n'existe pas : exception toleree (artefact visuel de navigation). RAPPORTE l'absence.\n\nCRITERE D'ACCEPTATION :\n- L'entree apparait entre Travailleurs et Actions.\n- L'icone `BookOpen` (ou alternative justifiee) est utilisee.\n- `npm run typecheck` passe.\n- `npm run test:run` passe.\n\nCOMMANDE DE VALIDATION (depuis repo root) :\n```\nnpm run typecheck && npm run test:run\n```\n\nCOMMIT GIT (apres validation OK) :\n```\ngit status --short\ngit add src/components/Sidebar.tsx\ngit add src/components/Sidebar.test.tsx   # SEULEMENT si modifie\ngit commit -m \"feat(sidebar): ajouter entree Competences\"\n```\nAUCUN trailer `Co-Authored-By`, AUCUN `--amend`, AUCUN `--no-verify`.\nCite le SHA court dans le rapport final.\n\nRAPPORT FINAL (3 lignes max) :\n- Ligne 1 : entree ajoutee + icone utilisee + test (oui/non)\n- Ligne 2 : resultat validation\n- Ligne 3 : SHA court"
  },
  {
    "id": 6,
    "titre": "Creer workflow GitHub Actions CI complet",
    "executor": "haiku",
    "executor_reason": "Workflow CI multi-jobs avec cache, conditions de trigger, build Windows + Strawberry Perl ; demande du jugement sur les chemins de cache, ordre des etapes et conditions.",
    "prompt": "Cree le workflow GitHub Actions `ci.yml` avec 4 jobs : lint+typecheck, test frontend, test Rust, build Windows.\n\nREPO ROOT : `C:\\work\\PCR Manager\\.claude\\worktrees\\compassionate-wilbur-9861c8`\n\nSCOPE STRICT (seules creations autorisees) :\n- `C:\\work\\PCR Manager\\.claude\\worktrees\\compassionate-wilbur-9861c8\\.github\\workflows\\ci.yml` (CREATION ; cree aussi les dossiers `.github\\workflows\\` s'ils n'existent pas).\n\nToute autre modification est INTERDITE. Si tu reperes un bug hors-scope, RAPPORTE-le sans corriger.\n\nINTERDIT : creer des helper scripts ad-hoc (`*.sh`, `*.ps1`, `*.bat`, `*.cmd`, `validate-*`, `run-*`, `test-runner*`, `check-*`). Le workflow s'execute uniquement via GitHub Actions ; aucun script local additionnel.\n\nLECTURE PREREQUISE OBLIGATOIRE :\n1. LIS `package.json` a la racine pour confirmer :\n   - Les scripts disponibles : `typecheck`, `lint`, `test:run`, `tauri:build`. Si un nom diffère, ADAPTE le workflow.\n   - La version Node recommandee (champ `engines.node` si present, sinon utilise Node 20 LTS).\n2. LIS `src-tauri\\Cargo.toml` pour la version Rust requise (champ `rust-version` si present, sinon `stable`).\n3. LIS `src-tauri\\tauri.conf.json` si necessaire pour les chemins de bundle (les `.exe` sont generes dans `src-tauri\\target\\release\\bundle\\nsis\\` par defaut).\n\nVERIFICATION DES VERSIONS DES ACTIONS GITHUB (OBLIGATOIRE) : utilise des versions ACTUELLES (au moment de la redaction) :\n- `actions/checkout@v4`\n- `actions/setup-node@v4`\n- `actions/cache@v4`\n- `actions/upload-artifact@v4`\n- Pour Rust : `dtolnay/rust-toolchain@stable` (preferer a `actions-rs/toolchain` qui est archive).\nNE PAS utiliser de versions `@v1` `@v2` deprecated.\n\nCONTENU DU WORKFLOW :\n\n```yaml\nname: CI\n\non:\n  push:\n    branches-ignore:\n      - 'dependabot/**'\n    tags:\n      - 'v*'\n  pull_request:\n    branches-ignore:\n      - 'dependabot/**'\n\njobs:\n  lint-typecheck:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: '20'\n          cache: 'npm'\n      - run: npm ci\n      - run: npm run typecheck\n      - run: npm run lint\n\n  test-frontend:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: '20'\n          cache: 'npm'\n      - run: npm ci\n      - run: npm run test:run\n\n  test-rust:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: dtolnay/rust-toolchain@stable\n      - uses: actions/cache@v4\n        with:\n          path: |\n            ~/.cargo/registry\n            ~/.cargo/git\n            src-tauri/target\n          key: ${{ runner.os }}-cargo-${{ hashFiles('src-tauri/Cargo.lock') }}\n          restore-keys: |\n            ${{ runner.os }}-cargo-\n      - run: cargo test --manifest-path src-tauri/Cargo.toml --lib\n\n  build-windows:\n    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))\n    runs-on: windows-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: '20'\n          cache: 'npm'\n      - uses: dtolnay/rust-toolchain@stable\n      - uses: actions/cache@v4\n        with:\n          path: |\n            ~/.cargo/registry\n            ~/.cargo/git\n            src-tauri/target\n          key: ${{ runner.os }}-cargo-${{ hashFiles('src-tauri/Cargo.lock') }}\n          restore-keys: |\n            ${{ runner.os }}-cargo-\n      - name: Add Strawberry Perl to PATH\n        shell: pwsh\n        run: echo \"C:\\Strawberry\\perl\\bin\" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append\n      - run: npm ci\n      - run: npm run tauri:build\n      - uses: actions/upload-artifact@v4\n        with:\n          name: pcr-manager-windows-installer\n          path: src-tauri/target/release/bundle/nsis/*.exe\n          if-no-files-found: error\n```\n\nADAPTATIONS POSSIBLES :\n- Si `package.json` ne contient pas exactement `tauri:build`, utilise le script equivalent (ex : `npm run tauri build` via `tauri-apps/tauri-action` ou `npx tauri build`).\n- Si la version Node attendue est differente (verifiee via `.nvmrc` si present), adapte la string `'20'`.\n\nTESTS ASSOCIES : Exception toleree — ce fichier EST la configuration de CI/tests. Aucun test unitaire propre n'est requis. Validation = parse YAML + structure valide.\n\nCRITERE D'ACCEPTATION :\n- `.github\\workflows\\ci.yml` existe et est un YAML valide.\n- Les 4 jobs sont presents avec les bonnes plateformes.\n- Le job `build-windows` ajoute Strawberry Perl en tete de PATH avant `npm run tauri:build`.\n- Les actions utilisent des versions @v4 (pas plus anciennes).\n\nCOMMANDE DE VALIDATION (depuis repo root) :\nValide la syntaxe YAML via un parser Node temporaire :\n```\nnpx --yes js-yaml .github/workflows/ci.yml\n```\nSortie attendue : impression du YAML parse, AUCUNE erreur. Si la commande echoue (parser absent), fallback :\n```\nnode -e \"const fs=require('fs');const c=fs.readFileSync('.github/workflows/ci.yml','utf8');if(!c.includes('lint-typecheck'))process.exit(1);if(!c.includes('test-rust'))process.exit(1);if(!c.includes('build-windows'))process.exit(1);if(!c.includes('Strawberry'))process.exit(1);console.log('structural checks OK')\"\n```\n\nCOMMIT GIT (apres validation OK) :\n```\ngit status --short\ngit add .github/workflows/ci.yml\ngit commit -m \"ci(github): ajouter workflow lint typecheck tests et build windows\"\n```\nAUCUN trailer `Co-Authored-By`, AUCUN `--amend`, AUCUN `--no-verify`.\nCite le SHA court dans le rapport final.\n\nRAPPORT FINAL (3 lignes max) :\n- Ligne 1 : workflow cree avec N jobs\n- Ligne 2 : resultat validation YAML\n- Ligne 3 : SHA court"
  }
]